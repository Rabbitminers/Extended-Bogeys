package com.rabbitminers.extendedbogeys.handler;

@Deprecated // Probably not needed, can be handled in RailwaySavedData
public class BogeyTypeHandler {

    // TODO: Implement Hashmap of bogey types, then serialize it to keep it when game is closed

    /*
    File file = new File("nameOfYourFile");
    FileOutputStream f = new FileOutputStream(file);
    ObjectOutputStream s = new ObjectOutputStream(f);
    s.writeObject(yourHashMap);
    s.close();
     */

    /*
    File file = new File("temp");
    FileInputStream f = new FileInputStream(file);
    ObjectInputStream s = new ObjectInputStream(f);
    HashMap<String, Object> fileObj2 = (HashMap<String, Object>) s.readObject();
    s.close();
     */
}
